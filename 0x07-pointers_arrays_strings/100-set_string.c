#include "main.h"
/**
 * set_string - main entry point
 * @s: a variable containing a value of a pointer
 * @to: a variable where the value is set to
 * Return: Always 0 success
 */

void set_string(char **s, char *to)
{
	*s = to;
}
